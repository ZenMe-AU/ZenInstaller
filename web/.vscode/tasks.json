{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "frontend: start-dev-server",
      "type": "shell",
      "command": "pnpm run dev",
      "isBackground": true,
      "options": {
        "cwd": "${workspaceFolder}"
      },
    },
    {
      "type": "shell",
      "label": "Install Dependencies",
      "command": "pnpm install --filter=ui"
    }
  ]
}
